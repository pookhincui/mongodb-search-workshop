# Prompt: Interactive Demo — "From Keywords to Meaning"

> วางทั้งไฟล์นี้ลงใน Augment Code ได้เลย ปรับ dataset/สีได้ตามต้องการ

---

## บทบาทและเป้าหมาย (Role & Goal)

คุณคือ frontend engineer ช่วยสร้าง **interactive demo ตัวเดียวจบ** สำหรับใช้บนเวที workshop หัวข้อ *"From Keywords to Meaning: Build a Smarter Search for AI"* ให้ลูกค้า AIS (ผู้ให้บริการ telecom ในไทย)

demo นี้จะถูก **ฉายบนจอใหญ่ต่อหน้าคน 50–70 คน** ผู้บรรยายจะพิมพ์คำค้นหรือคลิกคำค้นตัวอย่าง แล้วให้ทุกคนเห็นความต่างระหว่าง **การค้นแบบ Keyword (จับคู่ตัวอักษร)** กับ **การค้นแบบ Semantic (เข้าใจความหมาย)** แบบ side-by-side พร้อมกัน

**หมัดเด็ดของ demo:** มีคำค้นที่ Keyword หา "ไม่เจอเลย" (0 ผลลัพธ์) แต่ Semantic เจอคำตอบที่ถูกต้องทันที เพื่อสร้าง "aha moment"

---

## Deliverable

- ไฟล์เดียว: `index.html` — **vanilla HTML/CSS/JS, ไม่มี dependency ภายนอก, ไม่มี CDN, ไม่มี build step**
- ต้อง **ทำงานได้ 100% offline** (เปิดจากไฟล์บนเครื่องได้เลย) เพราะพรีเซนต์สดต้องชัวร์ ห้ามพังเพราะเน็ตหลุด
- ห้ามใช้ `localStorage`/`sessionStorage` (ไม่จำเป็น) เก็บ state ใน JS variable พอ

---

## Requirement เชิงฟังก์ชัน

1. ช่องค้นหา 1 ช่องด้านบน (ใช้ร่วมกันทั้งสองฝั่ง) — พิมพ์ครั้งเดียว อัปเดตทั้งสองฝั่งพร้อมกัน (debounce ~200ms)
2. แถว **preset query chips** ใต้ช่องค้นหา — คลิกแล้วเติมข้อความลงช่องค้นหาและรันทันที (นี่คือสิ่งที่ผู้บรรยายใช้จริงบนเวที)
3. Layout แบบ **split-screen 2 คอลัมน์**:
   - ซ้าย = **Keyword Search** — โทนเทา/ทื่อ สื่อว่าเป็นเครื่องจักรจับแค่ตัวอักษร
   - ขวา = **Semantic Search (AI)** — โทนเขียว/มีชีวิต สื่อว่าเข้าใจความหมาย
4. แต่ละฝั่งแสดงรายการผลลัพธ์ (title + snippet) เรียงตามคะแนนมากไปน้อย
5. **ฝั่ง Keyword:** ไฮไลต์คำ (สีเหลืองอำพัน) ในผลลัพธ์ที่ตรงตัวอักษรกับคำค้น; ถ้าไม่เจอให้แสดง empty state ชัดๆ เช่น "ไม่พบผลลัพธ์ — ไม่มีบทความที่มีตัวอักษรตรงกับคำค้นของคุณ"
6. **ฝั่ง Semantic:** แสดง chip "เข้าใจว่าคุณกำลังหมายถึง: [concept labels]" ไว้บนสุด แล้วใต้ผลลัพธ์แต่ละอันบอกว่า "ตรงกับความหมาย: [concept]" เพื่อสอน intuition ของ embeddings
7. **Scoreboard เปรียบเทียบ** (จุดพีคของ demo): แสดงชัดๆ ว่า `Keyword เจอ: N ผลลัพธ์` เทียบกับ `AI Semantic เจอ: M ผลลัพธ์`
8. Responsive: จอกว้างวาง 2 คอลัมน์, จอแคบ (มือถือ) วางซ้อนกัน

---

## ⚠️ Gotcha ทางเทคนิคที่ต้องระวัง (สำคัญมาก)

**ภาษาไทยไม่มีการเว้นวรรคระหว่างคำ** — จะ `split(" ")` แล้วจับคำไม่ได้ ให้ใช้วิธี **substring matching** แทน:
- ตรวจว่า term ปรากฏใน query ด้วย `query.includes(term)` (ไม่ใช่แยกคำจาก query)
- normalize ทุกอย่างด้วย `s => s.toLowerCase().trim()` ก่อนเทียบ (ไทยไม่กระทบ, อังกฤษให้ case-insensitive)

---

## Logic การค้นหา (ทำตามนี้เป๊ะ)

ให้ทุกเอกสารมีลิสต์ `kw` (คำที่ index แบบ keyword ธรรมดาจะเก็บไว้) และ `concepts` (แท็กความหมาย)

**Keyword Search** (ต่อเอกสาร):
```
matchedTerms   = doc.kw.filter(k => query.includes(normalize(k)))
keywordScore   = matchedTerms.length
```
- เอกสารจะโผล่ในผล Keyword ก็ต่อเมื่อ `keywordScore > 0`
- ไฮไลต์ `matchedTerms` ในผลลัพธ์
- เรียงตาม `keywordScore` มากไปน้อย

**Semantic Search** (ต่อเอกสาร):
```
detectedConcepts = concepts ที่มี trigger อย่างน้อย 1 ตัวเป็น substring ของ query
lexicalMatches   = doc.kw.filter(k => query.includes(normalize(k)))     // semantic รวม lexical ด้วย
sharedConcepts   = doc.concepts ∩ detectedConcepts
semanticScore    = sharedConcepts.length * 3  +  lexicalMatches.length * 1
```
- เอกสารจะโผล่ในผล Semantic ก็ต่อเมื่อ `semanticScore > 0`
- เรียงตาม `semanticScore` มากไปน้อย
- ผลลัพธ์นี้จะเป็น superset ที่ฉลาดกว่า keyword เสมอ (ตามที่ตั้งใจ)

---

## Dataset (ใช้ตามนี้ตรงๆ — คัดลอกไปได้เลย)

```js
const DOCUMENTS = [
  { id:"topup",       title:"ซื้อแพ็กเกจเน็ตเสริมเมื่อเน็ตหมด",
    snippet:"เมื่อเน็ตหมดกลางรอบบิล ซื้อแพ็กเกจเน็ตเสริมเพิ่มได้ทันทีผ่านแอป",
    kw:["แพ็กเกจ","เน็ต","เสริม","ซื้อ"], concepts:["data_pkg"] },

  { id:"social_fix",  title:"แก้ปัญหาเข้าแอปโซเชียลหรือเล่นแอปไม่ได้",
    snippet:"เปิดแอปโซเชียลไม่ขึ้น เล่นแอปไม่ได้ ลองตรวจการตั้งค่าเน็ตและสัญญาณ",
    kw:["โซเชียล","แอป","แก้ปัญหา"], concepts:["social_app","apn"] },

  { id:"bill",        title:"เช็กยอดค่าบริการและดูใบแจ้งหนี้",
    snippet:"ดูยอดค่าบริการรอบปัจจุบัน ประวัติการใช้ และใบแจ้งหนี้ย้อนหลัง",
    kw:["ยอด","ค่าบริการ","บิล","ใบแจ้งหนี้","เช็ก"], concepts:["bill"] },

  { id:"signal",      title:"แก้ปัญหาสัญญาณขาดหายและโทรไม่ติด",
    snippet:"สัญญาณอ่อน โทรออกไม่ได้ สายหลุดบ่อย ลองรีสตาร์ทและตรวจพื้นที่",
    kw:["สัญญาณ","โทร","แก้ปัญหา"], concepts:["signal"] },

  { id:"fiveg",       title:"วิธีเปิดใช้งาน 5G บนมือถือ",
    snippet:"ตรวจว่าเครื่องรองรับ 5G และเปิดใช้งานในการตั้งค่าเครือข่าย",
    kw:["5g","เปิด","มือถือ"], concepts:["fiveg","data_pkg"] },

  { id:"mnp",         title:"ย้ายค่ายมา AIS โดยใช้เบอร์เดิม",
    snippet:"ย้ายเครือข่ายมา AIS ใช้เบอร์เดิมได้ ไม่ต้องเปลี่ยนเบอร์",
    kw:["ย้ายค่าย","เบอร์เดิม","ais","ย้าย"], concepts:["mnp"] },

  { id:"topup_money", title:"เติมเงินและจ่ายบิลออนไลน์",
    snippet:"เติมเงินเข้าเบอร์และชำระค่าบริการออนไลน์ผ่านหลายช่องทาง",
    kw:["เติมเงิน","จ่าย","บิล","ชำระ"], concepts:["topup_money","bill"] },

  { id:"points",      title:"ใช้ AIS Points แลกของรางวัลและสิทธิพิเศษ",
    snippet:"สะสมและใช้พอยท์แลกส่วนลด ของรางวัล และสิทธิพิเศษ",
    kw:["points","แลก","รางวัล","สิทธิพิเศษ","พอยท์"], concepts:["points"] },

  { id:"apn",         title:"ตั้งค่าอินเทอร์เน็ตมือถือ (APN)",
    snippet:"เน็ตมือถือใช้ไม่ได้หลังเปลี่ยนเครื่อง ตั้งค่า APN ให้ถูกต้อง",
    kw:["ตั้งค่า","อินเทอร์เน็ต","apn","มือถือ"], concepts:["apn","data_pkg"] },

  { id:"unsub",       title:"ยกเลิกบริการเสริมและ SMS กวนใจ",
    snippet:"โดนหักเงินจากบริการที่ไม่ได้สมัคร ยกเลิกบริการเสริมและ SMS กวนใจ",
    kw:["ยกเลิก","บริการเสริม","sms"], concepts:["unsub"] },

  { id:"home",        title:"ติดตั้งเน็ตบ้าน AIS Fibre",
    snippet:"สมัครและนัดติดตั้งเน็ตบ้านไฟเบอร์ ตรวจพื้นที่ให้บริการ",
    kw:["เน็ตบ้าน","ไฟเบอร์","ติดตั้ง"], concepts:["home_net"] },

  { id:"roaming",     title:"เปิดโรมมิ่งใช้เน็ตและโทรเมื่อไปต่างประเทศ",
    snippet:"ก่อนเดินทางต่างประเทศ เปิดแพ็กโรมมิ่งเพื่อใช้เน็ตและโทรได้",
    kw:["โรมมิ่ง","ต่างประเทศ","เน็ต","โทร"], concepts:["roaming","data_pkg"] },
];
```

## Concept dictionary (semantic mapping)

```js
const CONCEPTS = [
  { id:"social_app",  label:"โซเชียล/แอป",
    triggers:["เฟส","เฟซ","facebook","fb","ไอจี","ig","instagram","อินสตา","ติ๊กต๊อก","tiktok","ไลน์","line","โซเชียล","social","ยูทูป","youtube","ทวิต","เล่นแอป","เล่นเกม"] },

  { id:"data_pkg",    label:"เน็ต/แพ็กเกจ",
    triggers:["เน็ตหมด","เน็ตช้า","เน็ตไม่พอ","ซื้อเน็ต","เติมเน็ต","แพ็ก","แพ็ค","ดาต้า","data","internet","อินเทอร์เน็ต","เน็ตแรง","เน็ต"] },

  { id:"bill",        label:"บิล/ยอดเงิน",
    triggers:["จ่าย","บิล","ยอด","ค่าบริการ","ค่าโทร","เท่าไหร่","เท่าไร","ค้างชำระ","ใบแจ้งหนี้","หนี้","ชำระ"] },

  { id:"signal",      label:"สัญญาณ",
    triggers:["สัญญาณ","โทรไม่ติด","โทรไม่ได้","สายหลุด","ไม่มีสัญญาณ","คลื่น","โทรออกไม่ได้"] },

  { id:"fiveg",       label:"5G",
    triggers:["5g","5 จี","ไฟว์จี"] },

  { id:"mnp",         label:"ย้ายค่าย",
    triggers:["ย้ายค่าย","ย้ายมา","เบอร์เดิม","ย้ายเครือข่าย","ย้ายซิม","ใช้เบอร์เดิม"] },

  { id:"roaming",     label:"โรมมิ่ง/ต่างประเทศ",
    triggers:["ต่างประเทศ","โรมมิ่ง","roaming","ไปเที่ยว","ญี่ปุ่น","เกาหลี","ยุโรป","ต่างแดน","เที่ยวเมืองนอก","บินไป"] },

  { id:"topup_money", label:"เติมเงิน",
    triggers:["เติมเงิน","เติมเบอร์","ท็อปอัพ","top up","เงินในซิม"] },

  { id:"points",      label:"พอยท์/รางวัล",
    triggers:["พอยท์","points","แต้ม","แลกของ","สะสมแต้ม","รางวัล","สิทธิพิเศษ"] },

  { id:"apn",         label:"ตั้งค่าเน็ต",
    triggers:["ตั้งค่าเน็ต","apn","ตั้งค่ามือถือ","เปลี่ยนเครื่อง"] },

  { id:"unsub",       label:"ยกเลิกบริการ",
    triggers:["ยกเลิก","ไม่ได้สมัคร","sms กวน","หักเงิน","บริการเสริม","คิดเงินแปลก"] },

  { id:"home_net",    label:"เน็ตบ้าน",
    triggers:["เน็ตบ้าน","ไฟเบอร์","fibre","fiber","wifi บ้าน","ติดตั้งเน็ต"] },
];
```

## Preset queries (แสดงเป็น chips ตามลำดับนี้)

```js
const PRESETS = [
  "เล่นเฟสไม่ได้",
  "จะบินไปญี่ปุ่นอาทิตย์หน้า",
  "โดนหักเงินทั้งที่ไม่ได้สมัคร",
  "เน็ตหมดกลางเดือน",
  "อยากรู้ว่าเดือนนี้จ่ายเท่าไหร่",
];
```

---

## Acceptance Tests (ต้องผ่านทุกข้อก่อนถือว่าเสร็จ)

รันคำค้นแต่ละอันแล้วผลต้องเป็นไปตามนี้ — 3 อันแรกคือ "money shot" (Keyword ต้องได้ 0):

| # | คำค้น | Keyword (ฝั่งซ้าย) | Semantic (ฝั่งขวา) — อันดับ 1 |
|---|-------|-------------------|-------------------------------|
| 1 | เล่นเฟสไม่ได้ | **0 ผลลัพธ์** (empty state) | แก้ปัญหาเข้าแอปโซเชียลฯ (concept: โซเชียล/แอป) |
| 2 | จะบินไปญี่ปุ่นอาทิตย์หน้า | **0 ผลลัพธ์** | เปิดโรมมิ่งฯ (concept: โรมมิ่ง/ต่างประเทศ) |
| 3 | โดนหักเงินทั้งที่ไม่ได้สมัคร | **0 ผลลัพธ์** | ยกเลิกบริการเสริมฯ (concept: ยกเลิกบริการ) |
| 4 | เน็ตหมดกลางเดือน | เจอหลายอันแบบปนๆ (มี "เน็ต" ตรงตัว รวมของที่ไม่ตรงใจ) | ซื้อแพ็กเกจเน็ตเสริมฯ ขึ้นบนสุด |
| 5 | อยากรู้ว่าเดือนนี้จ่ายเท่าไหร่ | เจอน้อย/ปนๆ | เช็กยอดค่าบริการฯ + เติมเงินและจ่ายบิลฯ |

ประเด็นการสอนที่ demo ต้องสื่อออกมาให้ได้: **money shot (1–3)** = keyword พังเพราะไม่มีคำตรงตัว; **ข้อ 4–5** = keyword เจอแบบมั่ว/ไม่ตรงใจ แต่ semantic จัดอันดับสิ่งที่ "ตั้งใจถาม" ไว้บนสุด

---

## Design direction

ไม่ใช่หน้า marketing แต่เป็น **"เครื่องมือเทียบผลแบบ split-screen"** ที่อ่านออกจากท้ายห้องได้ ตัวอักษรใหญ่ คอนทราสต์สูง (ห้องเปิดไฟ/ฉายโปรเจกเตอร์)

**Design tokens (แนะนำ ปรับได้):**
- `--ink: #0E1420` (ข้อความหลัก น้ำเงินเข้มเกือบดำ)
- `--paper: #F5F7F5` (พื้นหลังสว่าง เซฟสำหรับโปรเจกเตอร์)
- ฝั่ง Keyword (เทา ทื่อ): panel `#EEF0F1`, ตัวอักษร/ขอบ `#6B7280`
- ฝั่ง Semantic (เขียว มีชีวิต): accent `#12B24A`, เข้ม `#0A7A34`, พื้น chip เขียวอ่อน
- ไฮไลต์คำที่ตรงตัวอักษร (keyword): `#FFB020` (เหลืองอำพัน)

**Type:** ใช้ font stack ที่รองรับไทยและมีในเครื่องอยู่แล้ว (offline-safe) เช่น
`"Sukhumvit Set","Leelawadee UI","Noto Sans Thai",Tahoma,-apple-system,"Segoe UI",sans-serif`
ตั้ง type scale ให้ heading ใหญ่ชัด อ่านจากไกลได้

**Layout (wireframe):**
```
┌───────────────────────────────────────────────────────────┐
│  หัวข้อ + คำอธิบายหนึ่งบรรทัด                                │
│  ┌─────────────────────────────┐  🔎                        │
│  │  ช่องพิมพ์คำค้น               │                            │
│  └─────────────────────────────┘                            │
│  ตัวอย่าง:  [chip] [chip] [chip] [chip] [chip]              │
├──────────────────────────────┬────────────────────────────┤
│  KEYWORD (เทา)                │  SEMANTIC / AI (เขียว)      │
│  "จับคู่ตัวอักษร"              │  "เข้าใจว่าคุณหมายถึง: ▢ ▢" │
│  ────────────────             │  ─────────────────────      │
│  • ผลลัพธ์ (ไฮไลต์คำตรง)       │  • ผลลัพธ์ (+ ตรงกับ: concept)│
│  • ...                        │  • ...                       │
│  [ ถ้าว่าง → empty state ]     │                             │
├──────────────────────────────┴────────────────────────────┤
│  Keyword เจอ: 0   ·   AI Semantic เจอ: 3                   │
└───────────────────────────────────────────────────────────┘
```
จัดวางเนื้อหาชิดซ้าย, scoreboard วางกลางล่างให้เด่น

**Motion:** ใช้แค่จังหวะเดียว — ตอนผลลัพธ์เด้ง ให้ panel reveal เบาๆ และตัวเลข scoreboard นับขึ้นสั้นๆ; เคารพ `prefers-reduced-motion`

---

## Mobile self-play + Deploy/QR

demo นี้ต้องให้ผู้ฟัง **~30 คนสแกน QR แล้วเล่นบนมือถือตัวเองพร้อมกัน** ขณะที่ผู้บรรยายเล่นบนจอใหญ่ไปด้วย

**โมเดลการเล่น: self-play แยกเครื่อง (ไม่แชร์ state)**
- แต่ละมือถือโหลดไฟล์ HTML เดียวกันไปรันบนเครื่องตัวเอง คำค้นของแต่ละคนเป็นของใครของมัน ไม่ไหลไปจอกลาง
- นี่คือดีไซน์ที่ตั้งใจ: พลังของ demo คือให้แต่ละคนพิมพ์คำแปลกๆ ของตัวเองแล้วเห็น keyword พังกับตา — **ห้ามเพิ่ม backend/websocket/database เพื่อรวมคำค้นขึ้นจอกลาง** (เกินสโคป และทำให้เล่นพร้อมกันหลายคนแล้วรก)
- เพราะรัน client ล้วน จำนวนคนเล่นพร้อมกันจึงไม่กระทบภาระระบบ (static hosting รับได้สบาย)

**Mobile UX ที่ต้องทำให้ดี (จอเล็ก + นิ้วโป้ง):**
1. เพิ่ม `<meta name="viewport" content="width=device-width, initial-scale=1">`
2. บนจอแคบ: split-screen 2 คอลัมน์ **สลับเป็นวางซ้อนกัน** โดยให้ฝั่ง **Keyword อยู่บน / Semantic อยู่ล่าง** (จะได้เลื่อนเจอ "ตัวพัง" ก่อน แล้วค่อยเจอ "ตัวฉลาด" — จังหวะเล่าดีกว่า) และคง scoreboard เปรียบเทียบให้เห็นครบทั้งสองฝั่งบนจอเดียวถ้าเป็นไปได้
3. preset chips ต้องแตะง่ายด้วยนิ้วโป้ง: เป้าแตะสูง **อย่างน้อย 44px**, ระยะห่างระหว่าง chip พอ, ตัด chip ขึ้นบรรทัดใหม่ได้ (wrap) ไม่ใช่ scroll แนวนอนอย่างเดียว
4. ช่องพิมพ์ต้อง **ไม่โดนคีย์บอร์ดมือถือบัง** — ให้ช่องค้นหากับ scoreboard อยู่ส่วนบนของหน้า, ใช้ `font-size: 16px` ขึ้นไปกับ input (กัน iOS ซูมเองตอนโฟกัส), และตั้ง `inputmode`/`enterkeyhint="search"` ให้เหมาะ
5. ตัวอักษรผลลัพธ์อ่านง่ายบนจอเล็ก (body ≥ 15px), คอนทราสต์ผ่าน WCAG AA, ปุ่ม/ผลลัพธ์กดแล้วมี feedback
6. รองรับ dynamic viewport (`100dvh` แทน `100vh`) กัน address bar มือถือกินพื้นที่
7. ทดสอบด้วย responsive/emulator ที่ความกว้าง ~375px ว่าเล่นครบทุกฟังก์ชันโดยไม่ต้องซูม

**Deploy + QR (ให้ Augment Code ทำให้เสร็จในตัว):**
- โฮสต์ไฟล์ที่ **URL สาธารณะ** (มือถือคนอื่นเข้าถึง `file://` บนเครื่องผู้บรรยายไม่ได้ ต้องมี URL จริง)
- แนะนำ **GitHub Pages** (เข้ากับ workflow ที่ใช้ repo อยู่แล้ว): commit `index.html` ที่ root แล้วเปิด Pages ให้ deploy จาก branch หลัก — ทางเลือกอื่นที่ลากวางได้เร็ว: Netlify Drop / Cloudflare Pages
- หลังได้ URL ให้ **สร้าง QR code แบบ offline** (เช่นสร้างไฟล์ `qr.html` ที่ render QR ด้วย JS ล้วนแบบ vendored ไม่พึ่ง external API เพื่อไม่ให้ QR พังถ้าเน็ตล่ม) หรืออย่างน้อย **พิมพ์ URL สุดท้ายออกมาให้ชัด** เพื่อเอาไปทำ QR/ใส่สไลด์เอง
- ทำ `README.md` สั้นๆ บอกขั้นตอน deploy + ที่อยู่ QR/URL

**เผื่อไว้กันงานล่ม (resilience):**
- ผู้ฟังต้องมีเน็ตแค่ตอน **โหลดครั้งแรก** ไฟล์เดียวขนาดเล็กจึงโหลดไว แม้สัญญาณอ่อน; โหลดเสร็จแล้วเล่นต่อได้แม้เน็ตหลุด
- เขียนโค้ดให้ทุก asset (font-stack, JS, data) อยู่ในไฟล์เดียว/inline ทั้งหมด ไม่มี request ออกนอก ตอน runtime

---

## สิ่งที่ "ไม่ต้อง" ทำ

- ไม่ต้องใช้ ML model จริง / embeddings API — ใช้ concept dictionary ด้านบนก็พอ (เชื่อถือได้ ควบคุมได้ 100% สำหรับเวที)
- ไม่ต้องเรียก backend หรือ network ใดๆ
- ไม่ต้องลอก logo/แบรนด์จริงของ AIS — ใช้สีเขียวกลางๆ และระบุว่าเป็น demo dataset
- ไม่ต้องทำ routing/หลายหน้า — หน้าเดียวจบ

## Definition of Done

- เปิด `index.html` แบบ offline แล้ว: พิมพ์/คลิกครบทั้ง 5 preset, ผลตรงตาราง acceptance test, ไม่มี error ใน console, ไม่มีการเรียก network ตอน runtime
- ที่ความกว้าง ~375px (มือถือ): เล่นครบทุกฟังก์ชันโดยไม่ต้องซูม, chip แตะง่าย, ช่องพิมพ์ไม่โดนคีย์บอร์ดบัง, Keyword อยู่บน/Semantic อยู่ล่าง
- Deploy ขึ้น URL สาธารณะสำเร็จ, มี QR (หรือ URL ชัดๆ) ให้ผู้ฟังสแกน, และมี `README.md` บอกขั้นตอน
- ทดสอบสแกนจากมือถือจริง 1 เครื่องแล้วโหลดและเล่นได้
